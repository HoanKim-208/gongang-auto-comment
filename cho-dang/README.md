# Reel chờ hẹn giờ

Bỏ vào đây cặp file cùng tên `<slug>.mp4` + `<slug>.json`. Workflow "Hẹn giờ Reel Gọn Gàng Shop" tự đẩy lên Facebook, hẹn giờ, thêm comment vào `lich.json`, rồi chuyển json sang `da-hen/`.

Mẫu json và quy trình: xem đầu `dang_reel.py`.
